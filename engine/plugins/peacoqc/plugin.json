{
  "id": "peacoqc",
  "name": "PeacoQC (quality control)",
  "version": "1.0.0",
  "description": "Flags and removes events in unstable regions of the acquisition (flow-rate and signal-stability anomalies) using PeacoQC. Returns the kept event indices, which StreamFLOW applies as a cleaned sub-population.",
  "vendor": "PeacoQC (vendored: python-migration/PeacoQC-master)",
  "status": "stable",
  "requires": {
    "r": ">=4.0",
    "packages": ["jsonlite", "flowCore", "PeacoQC"]
  },
  "entry": {
    "script": "peacoqc.R",
    "function": "streamflow_run"
  },
  "params": [
    { "id": "channels", "label": "Channels to monitor", "type": "channels", "required": false,
      "help": "Leave empty to monitor every channel except Time." },
    { "id": "mad", "label": "MAD threshold", "type": "double", "default": 6, "min": 1, "max": 20,
      "help": "Lower is stricter (PeacoQC default 6)." },
    { "id": "it_limit", "label": "IsolationTree limit", "type": "double", "default": 0.6, "min": 0.1, "max": 1.0,
      "help": "PeacoQC default 0.6." }
  ],
  "outputs": ["kept_indices", "n_removed", "pct_removed"]
}
